@inherits BaseInputGroup<TValue>
@typeparam TOption
@typeparam TValue where TValue : ISet<TOption>, new()

<div @attributes="AdditionalAttributes" class="@CssClass">
    <TemplateInputGroup DisplayName="@DisplayNameCurrent" Description="@DescriptionCurrent" ErrorList="ErrorList">
        <InputElementCheckboxSet @attributes="InputAttributes" Name="@GetName()" OptionList="OptionList" Value="Value" ValueChanged="ValueChanged" ValueExpression="ValueExpression">
            <ChildContent>
                @ChildContent(context)
            </ChildContent>
            <Control>
                @if (Control != null)
                {
                    @Control(context)
                }
                else
                {
                    <InputControlCheckbox Checked="@context.Checked"/>
                    <InputElementCheckbox ID="@context.ID" Name="@context.Name" Option="@context.Option" Checked="@context.Checked" OnChange="x => context.OnChange(x, context.Option)"/>
                }
            </Control>
        </InputElementCheckboxSet>
    </TemplateInputGroup>
</div>

@code {

    [Parameter, EditorRequired]
    public required RenderFragment<InputElementCheckboxSetScope<TOption>> ChildContent { get; set; }

    [Parameter]
    public RenderFragment<InputElementCheckboxSetScope<TOption>>? Control { get; set; }

    [Parameter, EditorRequired]
    public required HashSet<TOption> OptionList { get; set; }

}
